#include <stdio.h>

/**
 * main - prints all single digit number in base of 10
 * Return: 0(Always Successful)
 */
int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
		printf(n);
	printf('\n');
	return (0);
}
